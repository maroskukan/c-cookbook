#include <stdio.h>

int main()
{
    int a,b,c;

    printf("Type tree letters: ");
    a = getchar();
    putchar(a);
    b = getchar();
    putchar(b);
    c = getchar();
    putchar(c);

    return(0);
}